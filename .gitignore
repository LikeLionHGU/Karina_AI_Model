# Python 가상 환경
# 가상 환경은 사용자 컴퓨터마다 다르며, 라이브러리가 설치되는 공간이므로 공유할 필요가 없습니다.
venv/

# IDE 및 편집기 설정 파일
# PyCharm, VS Code 등의 개인 설정 파일은 다른 개발자와 충돌을 일으킬 수 있습니다.
.idea/
.vscode/

# Python 캐시 파일
# 파이썬 실행 시 자동으로 생성되는 임시 파일입니다.
__pycache__/
*.pyc

# Jupyter Notebook 체크포인트
# 노트북 실행 시 자동으로 생성되는 백업 파일입니다.
.ipynb_checkpoints/

# 훈련된 모델 파일 및 데이터 (대용량 바이너리 파일)
# 모델 가중치 파일은 용량이 매우 크므로 Git LFS를 사용하거나, 저장소에 포함하지 않는 것이 좋습니다.
*.pt
*.ckpt
*.pth

# 데이터 파일 (이미지, 동영상 등)
# 프로젝트의 소스 코드가 아닌, 훈련/테스트에 사용되는 데이터는 저장소에서 제외하는 것이 좋습니다.
# 현재 보이는 1.jpg, 2.jpg 등이 여기에 해당됩니다.
*.jpg
*.jpeg
*.png

# 로그 및 기타 생성 파일
*.log
dockerfile
.dockerignore
